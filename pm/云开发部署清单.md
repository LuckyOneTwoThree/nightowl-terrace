# 云开发部署清单（按顺序操作）

> 环境 ID：`cloudbase-d3gvu54t8fbbb6b3f`
> 在微信开发者工具 → 云开发控制台操作

---

## 一、创建数据库集合（10 个）

云开发控制台 → 数据库 → 新建集合，逐个创建：

| 集合名 | 权限 | 说明 |
|---|---|---|
| `fixtures` | 所有用户可读，仅创建者可写 | 赛程（服务端写入，客户端只读） |
| `recommendations` | 所有用户可读，仅创建者可写 | 推荐看点 |
| `storylines` | 所有用户可读，仅创建者可写 | 故事线 |
| `predictions` | **仅创建者可读写** | 盲评猜球（防偷看的根基：只能写自己的，看不到别人的） |
| `checkins` | **仅创建者可读写** | 夜猫打卡 |
| `boasts` | **仅创建者可读写** | 德比法庭留言 |
| `users` | 仅创建者可读写 | 用户昵称与偏好 |
| `subscriptions` | **仅创建者可读写** | 订阅消息授权记录（pushReminders 用） |
| `standings` | 所有用户可读，仅创建者可写 | 赛季总榜（云函数写入） |
| `weeklyReports` | 所有用户可读，仅创建者可写 | 周报快照 |

**关键**：predictions / checkins / boasts 三个集合必须设为「仅创建者可读写」——这是防偷看的根基，用户只能写自己的数据，看不到别人的，排行榜必须走云函数中转。

---

## 二、部署云函数（4 个）

微信开发者工具 → 右键 `cloudfunctions/` 目录下每个文件夹 → 「上传并部署：云端安装依赖」

按顺序部署：

1. `settleMatches` —— 比分结算
2. `readBoard` —— 榜单读取
3. `weeklyReport` —— 周报生成（含定时触发器）
4. `pushReminders` —— 开球提醒（含定时触发器）

部署后控制台会显示「上传成功」。定时触发器在 `config.json` 里已配好，部署时自动生效。

> `pushReminders` 部署后需在控制台「云函数 → 配置 → 环境变量」补充：
> - `TMPL_KICKOFF`（开球提醒订阅消息模板 ID）
> - `TMPL_DEADLINE`（盲评截止提醒订阅消息模板 ID）
>
> 模板未过审前保持未配置即可——函数会安全空转，客户端已走 ICS 日历导出兜底。

---

## 三、导入数据（3 个只读集合）

先在仓库根目录运行脚本生成云端导入文件（NDJSON 格式，已自动补 `_id` 与 `settled` 字段）：

```bash
node tools/export_cloud_data.js
```

再到云开发控制台 → 数据库 → 对应集合 → 导入，逐个选择 `cloud_data/` 下的文件（脚本共生成 7 个文件，云函数当前只读取以下 3 个，其余为后续功能预留，无需导入）：

### fixtures（1,753 条）
源文件：`cloud_data/fixtures.json`

### recommendations（24 条）
源文件：`cloud_data/recommendations.json`（注意：以 `_id = 比赛id` 稳定主键，重复导入会提示主键冲突，属预期；如需重导请先清空集合）

### storylines（9 条）
源文件：`cloud_data/storylines.json`

---

## 四、验证（部署后自测）

1. 控制台 → 云函数 → `settleMatches` → 测试 → 传空参数 `{}` 运行
   - 期望返回 `{ ok: true, matches: 4, predictions: 0, void: 0, standings: 0 }`
   - `matches: 4` = 识别并结算 4 场 `st:'done'` 已赛场次（再次运行会变成 `matches: 0`，因已被标记 settled）；`predictions: 0` = 暂无用户预测，空转正常
   - 注：settleMatches 已配置每小时定时触发（`config.json`），日常无需手动运行
2. 控制台 → 云函数 → `readBoard` → 测试 → 传 `{ "board": "owl", "gid": "test" }`
   - 期望返回 `{ ok: true, board: "owl", list: [], worst: null }`
3. 控制台 → 云函数 → `weeklyReport` → 测试 → 传空参数 `{}`
   - 期望返回 `{ ok: true, week: "2026-08-10", groups: 0, cards: [] }`

---

## 五、客户端联调点（我后续代码改造）

云函数就绪后，客户端需要从「本地存储」切换到「云函数调用」：
- predict 页：封存时写入 `predictions` 集合（仍保留本地哈希）
- board 页：读榜单改调 `wx.cloud.callFunction({ name: 'readBoard', data: { board: 'owl', gid } })`
- records 页：结算改由 `settleMatches` 云函数自动完成，本地只做展示
- court 页：留言写入 `boasts` 集合

**降级策略**：云函数调用失败时，自动回退本地存储，不影响基础体验。

---

*部署遇到任何问题截图发我，我来定位。*
