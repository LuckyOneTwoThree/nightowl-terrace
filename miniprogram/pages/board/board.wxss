.page { padding: 24rpx 32rpx 64rpx; min-height: 100vh; }

/* hero */
.hero-head { display: flex; align-items: center; justify-content: space-between; padding: 12rpx 8rpx 20rpx; }
.hero-title { font-size: 48rpx; font-weight: 700; color: var(--on-surface); }
.hero-moon { width: 52rpx; height: 52rpx; }

.stat { border-radius: var(--r-xl); padding: 28rpx 32rpx; display: flex; flex-direction: column; gap: 16rpx; }
.stat-sub { font-size: 22rpx; }
.stat-row { display: flex; align-items: center; justify-content: space-around; }
.stat-item { display: flex; flex-direction: column; align-items: center; gap: 4rpx; }
.stat-u { font-size: 20rpx; }
.stat-div { width: 2rpx; height: 48rpx; background: var(--sc-highest); }

/* 打卡卡 */
.live { position: relative; overflow: hidden; margin-top: 28rpx; padding: 28rpx 32rpx; border-radius: var(--r-xl); }
.accent-bar { position: absolute; left: 0; top: 0; bottom: 0; width: 8rpx; background: var(--secondary); box-shadow: 0 0 12rpx rgba(68,226,205,.5); }
.accent-bar.gray { background: var(--outline-muted); box-shadow: none; }
.live-status { display: flex; align-items: center; gap: 12rpx; margin-bottom: 24rpx; }
.live-status .mono { font-size: 22rpx; }

.live-vs { display: flex; align-items: center; justify-content: space-between; padding: 10rpx 0 20rpx; }
.lv-side { display: flex; flex-direction: column; align-items: center; gap: 12rpx; width: 160rpx; flex-shrink: 0; }
.lv-name { font-size: 26rpx; color: var(--on-surface); text-align: center; max-width: 160rpx; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.lv-mid { flex: 1; min-width: 0; display: flex; flex-direction: column; align-items: center; gap: 10rpx; }
.score-m.sm { font-size: 44rpx; white-space: nowrap; }
.match-time { font-size: 32rpx; font-weight: 700; white-space: nowrap; }

.live .btn-amber { margin-top: 24rpx; }
.live .btn-amber.done { opacity: .55; box-shadow: none; }
.live-note { display: block; text-align: center; margin-top: 16rpx; font-size: 22rpx; }

/* 排名 */
.sect-gap { margin: 48rpx 0 20rpx; }
.rank-card { padding: 6rpx 0; overflow: hidden; border-radius: var(--r-xl); }
.rank-row {
  display: flex; align-items: center; gap: 20rpx; padding: 22rpx 28rpx;
  border-bottom: 1rpx solid rgba(159,142,121,.08);
}
.rank-row:last-child { border-bottom: none; }
.rk-no { width: 48rpx; text-align: center; font-size: 32rpx; color: var(--on-surface-variant); }
.rank-1 { color: #FFB224; font-size: 36rpx; }
.rank-2 { color: #E0E2EA; font-size: 34rpx; }
.rank-3 { color: #CD7F32; font-size: 34rpx; }

.rk-avatar { flex-shrink: 0; font-size: 20rpx; }
.rk-info { flex: 1; display: flex; flex-direction: column; gap: 4rpx; }
.rk-name { font-size: 28rpx; color: var(--on-surface); }
.rk-s { font-size: 20rpx; }
.rk-h { font-size: 30rpx; }

.user-row { margin: 12rpx; border-radius: var(--r-lg); border: 1rpx solid rgba(255,178,36,.35); }
.user-rank { color: var(--primary-container); font-size: 32rpx; }
.user-avatar { background: var(--primary-container); color: var(--on-primary); }

/* 周报卡片 */
.report { margin-top: 36rpx; padding: 36rpx 32rpx; display: flex; flex-direction: column; align-items: center; gap: 14rpx; border-radius: var(--r-xl); }
.rp-head { display: flex; align-items: center; gap: 12rpx; }
.rp-ic { width: 44rpx; height: 44rpx; }
.rp-sub { font-size: 24rpx; }
.rp-worst {
  display: flex; align-items: center; gap: 10rpx;
  margin-top: 8rpx; padding: 12rpx 28rpx; border-radius: var(--r-full);
  background: rgba(255,180,171,.08); border: 1rpx solid rgba(255,180,171,.3);
}
.rp-worst-ic { width: 28rpx; height: 28rpx; }
.rp-worst-text { font-size: 22rpx; color: var(--error); }
.rp-btn { margin-top: 16rpx; width: 80%; }
