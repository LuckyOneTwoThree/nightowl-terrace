.page { padding-bottom: 64rpx; min-height: 100vh; }

/* 顶部吸顶控制栏 (Sticky Header) */
.sticky-bar {
  position: -webkit-sticky;
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(16, 20, 25, 0.95);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1rpx solid rgba(159, 142, 121, 0.12);
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.45);
}

/* 视图切换（日历 / 轮次） */
.vtoggle { display: flex; gap: 12rpx; padding: 16rpx 32rpx 6rpx; }
.vt-btn {
  flex: 0 0 auto; padding: 10rpx 28rpx; border-radius: var(--r-full);
  background: var(--surface-2); border: 1rpx solid rgba(159,142,121,.18);
  color: var(--on-surface-variant); font-size: 24rpx;
  transition: all .15s ease;
}
.vt-btn:active { background: var(--sc-high); }
.vt-btn.on {
  background: rgba(255,178,36,.12); border-color: rgba(255,178,36,.45);
  color: var(--primary-container); font-weight: 700;
}

/* 日期条 */
.dstrip { white-space: nowrap; padding: 14rpx 32rpx 10rpx; width: 100%; box-sizing: border-box; }
.dchip {
  position: relative; display: inline-flex; flex-direction: column; align-items: center; justify-content: center; gap: 6rpx;
  width: 136rpx; padding: 18rpx 0; margin-right: 16rpx;
  border-radius: var(--r-lg); background: var(--sc-low); border: 1rpx solid var(--outline-muted);
  transition: all .2s ease;
}
.dchip-dot { position: absolute; top: 10rpx; right: 12rpx; width: 10rpx; height: 10rpx; }
.dchip-wd { font-size: 22rpx; color: var(--on-surface-variant); font-weight: 500; }
.dchip-md { font-size: 26rpx; font-weight: 600; }
.dchip-count { font-size: 20rpx; }
.dchip.on {
  background: var(--primary-container); border-color: transparent;
  box-shadow: 0 4rpx 24rpx rgba(255,178,36,.3);
}
.dchip.on .dchip-wd, .dchip.on .dchip-md, .dchip.on .dchip-count {
  color: var(--on-primary); font-weight: 700;
}

/* 联赛药丸 */
.pills { display: flex; white-space: nowrap; padding: 8rpx 32rpx 18rpx; width: 100%; box-sizing: border-box; }
.pills .fpill { display: inline-flex; margin-right: 16rpx; }
.star-pill { font-weight: 600; }


/* 分组 */
.list { padding: 4rpx 32rpx 0; }
.group { scroll-margin-top: 20rpx; margin-bottom: 32rpx; }
.g-head { display: flex; align-items: baseline; gap: 16rpx; padding: 20rpx 0 16rpx; }
.g-date { font-size: 34rpx; font-weight: 700; color: var(--on-surface); }
.g-week { font-size: 24rpx; }
.g-line { flex: 1; height: 2rpx; background: linear-gradient(90deg, rgba(159,142,121,.25) 0%, rgba(159,142,121,0) 100%); }

/* 赛卡 */
.match {
  position: relative; display: flex; align-items: center;
  margin-bottom: 20rpx; padding: 0; overflow: hidden;
  border-radius: var(--r-xl);
  transition: transform .15s ease;
}
.match:active { transform: scale(0.99); background: var(--sc); }
.m-accent { position: absolute; left: 0; top: 0; bottom: 0; width: 8rpx; }
.m-body { flex: 1; padding: 22rpx 24rpx 22rpx 32rpx; }

.m-top { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; gap: 10rpx; }
.m-top-left { display: flex; align-items: center; gap: 12rpx; }
.m-lg-name { font-size: 22rpx; color: var(--on-surface-variant); font-weight: 600; }
.m-status-chip { padding: 2rpx 14rpx; font-size: 18rpx; }
.m-top-right { display: flex; align-items: center; gap: 12rpx; }
.local-txt { font-size: 20rpx; }
.m-hm { font-size: 26rpx; font-weight: 700; }

.t-row { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12rpx; }
.t-row:last-child { margin-bottom: 0; }
.t-name { font-size: 28rpx; font-weight: 600; color: var(--on-surface); }
.dim2 { color: var(--on-surface-variant); font-weight: 400; }
.m-team-info { display: flex; align-items: center; gap: 16rpx; }
.score { font-size: 32rpx; width: 36rpx; text-align: center; }
.score.ft { color: var(--on-surface); font-weight: 700; }
.score.dim { color: var(--on-surface-variant); opacity: .6; }

.m-side {
  display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 14rpx;
  padding: 0 24rpx 0 12rpx; border-left: 1rpx solid rgba(159,142,121,.1);
}
.stars-col { display: flex; flex-direction: column; gap: 2rpx; }
.star-gold { color: var(--primary-container); font-size: 20rpx; line-height: 1; }
.st-btn { padding: 8rpx; border-radius: var(--r-full); }
.st-icon { width: 32rpx; height: 32rpx; color: var(--on-surface-variant); }

.g-empty { padding: 36rpx; text-align: center; margin-bottom: 16rpx; }
.foot { text-align: center; padding: 36rpx 0 16rpx; font-size: 22rpx; }

